require "spec_helper"

RSpec.describe Argonaut do
  it "has a version number" do
    expect(Argonaut::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
